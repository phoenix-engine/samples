# Sample 2: BasicWorld2D

This sample introduces a simple 2D World and Renderer.

[sample/render2d.hxx]

As before, we first create a Phoenix object.  This fails because the SDL window
isn't configured properly!
